# Cara install yang disarankan: `pip install -e ".[api,dev]"` (lihat pyproject.toml).
# File ini disediakan untuk yang belum terbiasa dengan `pip install -e .` —
# isinya harus tetap disamakan manual dengan [project.dependencies] di pyproject.toml.

# Orkestrasi RAG
langchain==0.3.27
langchain-community==0.3.27
langchain-text-splitters==0.3.11

# Vector store
langchain-chroma==0.2.6
chromadb>=1.0.20

# Embedding
langchain-huggingface==0.3.1
sentence-transformers==3.3.1

# Pembaca PDF
pypdf==5.1.0

# Klien LLM Qwen (kompatibel OpenAI)
openai==1.59.6

# Konfigurasi & validasi
python-dotenv==1.0.1
pydantic>=2.6
pydantic-settings>=2.2

# Opsional — hanya perlu kalau menjalankan sadewa_ai/api.py
fastapi>=0.110
uvicorn[standard]>=0.27

# Pengujian
pytest==8.3.4
httpx>=0.27
